# Checklist

- Does the call site read naturally?
- Does the exported symbol repeat the package concept?
- Is the package name short, lower-case, and specific?
- Are receiver names short and consistent?
- Are initialisms consistently capitalized?
- Are exported comments useful to an API consumer?
- Is each interface owned by the consumer and small enough?
- Would returning a concrete type simplify the API?
